<h1>Burgers!
<img src="http://localhost:3000/assets/img/hamburger-2201748_640.jpg" style="width: 50px; height: 50px" </img>
</h1>

<div>
<div style="display: inline-block; vertical-align : top">
<h2>Burgers that are not devoured!</h2>
<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burger/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div style="display: inline-block; vertical-align: top; margin-left: 150px">
<h2>Burgers that are devoured!</h2>
<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burger/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>

<h2>Add a Burger</h2>
<form class="create-form">
    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="burger_id" name="name">
    </div>

    <button type="submit">Add Burger</button>
</form>